# ── Server ──────────────────────────────────────
PORT=5000
NODE_ENV=development

# ── Database ─────────────────────────────────────
# Get this from: https://cloud.mongodb.com → Connect → Drivers
MONGO_URI=mongodb+srv://Piyush:Ptaneja34@cluster0.hzdomht.mongodb.net/smartcart?appName=Cluster0

# ── Auth ──────────────────────────────────────────
# Any long random string, e.g. run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRE=7d

# ── AI (Anthropic Claude) ─────────────────────────
# Get this from: https://console.anthropic.com/account/keys
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── Client URL (for CORS) ─────────────────────────
CLIENT_URL=http://localhost:5173
